@model IEnumerable<ASF.Entities.Category>

<p>
    {{'Categoria' | translate}}
</p>
<table class="table">
    <tr>
        <th>
           
        </th>
       
       
    </tr>

    @foreach (var item in Model) {
        <tr>
            <td>
                <a href="/Product/ProductosXCategoria/@item.Id ">@Html.DisplayFor(modelItem => item.Name)</a> 
            </td>
       
            @*<td>
          
            @Html.ActionLink("Buscar Productos", "Details", new { id=item.Id }) |
          
        </td>*@
        </tr>
    }

</table>